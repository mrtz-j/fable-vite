{
    "private": true,
    "scripts": {
        "postinstall": "dotnet tool restore",
        "start": "dotnet fable watch src -s --run vite",
        "build": "dotnet fable src -s --run vite build",
        "serve": "vite serve"
    },
    "devDependencies": {
        "vite": "^4.0.3"
    },
    "version": "1.0.0"
}
